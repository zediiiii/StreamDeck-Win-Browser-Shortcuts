# Screenshots

The README references four images. Capture these on a clean install rather than
a configured machine, so what readers see matches what they get.

| File                | Shows                                                    |
|---------------------|----------------------------------------------------------|
| `01-folder.png`     | The unzipped folder, with `Start Here.cmd` visible        |
| `02-main-window.png`| The tool with a few rows ticked and the Match? column lit |
| `03-streamdeck-folder.png` | `TabButtons\StreamDeck` in Explorer               |
| `04-streamdeck-app.png`    | The Stream Deck app, System > Open highlighted    |

Then replace the `*(screenshot: ...)*` lines in README.md with:

```markdown
![Folder contents](docs/01-folder.png)
```
